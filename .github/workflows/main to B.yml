name: 🚀 Auto Update Network Nodes

on:
  workflow_dispatch: 
  schedule:
    - cron: '0 0 * * *' 
  push:
    paths:
      - 'sources.yaml'   
      - 'Nodes/**'       

jobs:
  run-all:
    runs-on: ubuntu-24.04
    permissions:
      contents: write 

    steps:
      - name: 1. Checkout Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: 2. Setup Python 3.10
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: 3. Install Python Dependencies
        run: pip install requests pyyaml

      - name: 4. Install & Connect Cloudflare WARP
        run: |
          curl -fsSL https://pkg.cloudflareclient.com/pubkey.gpg | sudo gpg --yes --dearmor --output /usr/share/keyrings/cloudflare-warp-archive-keyring.gpg
          echo "deb [signed-by=/usr/share/keyrings/cloudflare-warp-archive-keyring.gpg] https://pkg.cloudflareclient.com/ $(lsb_release -cs) main" | sudo tee /etc/apt/sources.list.d/cloudflare-client.list
          sudo apt-get update && sudo apt-get install cloudflare-warp -y
          warp-cli --accept-tos registration new
          warp-cli --accept-tos mode warp
          warp-cli --accept-tos connect
          sleep 5
          curl -4 ifconfig.me

      - name: 5. Run fetch.py (执行动态抓取与配置自我净化)
        run: python fetch.py

      - name: 6. Subconverter Parse
        run: |
          docker run -d --network host cmliu/subconverter:latest
          sleep 5
          fuser -k 8000/tcp || true
          python -m http.server 8000 &
          sleep 2
          API_URL=$(cat sub_api_url.txt)
          curl -s "$API_URL" > clash_nodes.yaml

      - name: 7. Run Mihomo Speed Test
        run: |
          wget https://github.com/MetaCubeX/mihomo/releases/download/v1.18.3/mihomo-linux-amd64-v1.18.3.gz
          gunzip mihomo-linux-amd64-v1.18.3.gz
          mv mihomo-linux-amd64-v1.18.3 mihomo
          chmod +x mihomo
          python check.py

      - name: 8. Generate Universal Base64 Sub
        run: |
          curl -s "http://127.0.0.1:25500/sub?target=mixed&url=http://127.0.0.1:8000/final_sub.yaml" > final_sub.txt

      - name: 9. Clean A Repo (清理当前 A 仓库现场)
        run: |
          git config --local user.email "actions@github.com"
          git config --local user.name "GitHub Action"
          
          # 清理 Nodes 文件夹内临时文件
          rm -f Nodes/* || true
          touch Nodes/.gitkeep
          
          # 强行从 git 追踪中删除可能存在的 tg_nodes.txt
          git rm --cached tg_nodes.txt || true
          
          # 🌟 A 仓库现在只负责保存净化后的 sources.yaml 和干净的 Nodes 文件夹！
          # 不再把 final_sub 提交到 A 仓库
          git add sources.yaml Nodes/ || true
          
          git commit -m "♻️ Cleaned Nodes Dir & Updated Config: $(date) [skip ci]" || echo "No changes to commit in A Repo"
          git pull origin main --rebase=false -X ours || true
          git push

      - name: 10. Push Results to B Repo (推送最终结果到 B 仓库)
        env:
          # 提取你设置的环境变量密钥
          GW124_TOKEN: ${{ secrets.GW124_TOKEN }}
        run: |
          # 1. 用 Token 克隆 B 仓库到临时文件夹 b_repo
          git clone https://${GW124_TOKEN}@github.com/gw124/Collection.git b_repo
          
          # 2. 确保 B 仓库中指定的目录存在，如果不存在则自动创建
          mkdir -p b_repo/Web/Nodes
          
          # 3. 把刚刚生成的最终节点文件复制过去
          cp final_sub.yaml b_repo/Web/Nodes/
          cp final_sub.txt b_repo/Web/Nodes/
          
          # 4. 进入 B 仓库目录，执行提交并推送
          cd b_repo
          git config --local user.email "actions@github.com"
          git config --local user.name "GitHub Action"
          
          git add Web/Nodes/final_sub.yaml Web/Nodes/final_sub.txt
          git commit -m "🚀 Auto update High-Speed nodes: $(date)" || echo "No changes to commit in B Repo"
          git push
